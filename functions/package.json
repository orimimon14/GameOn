{
  "name": "swish-game-functions",
  "private": true,
  "description": "Swish & Game Cloud Functions — backend-authoritative logic per docs/architecture/API_CONTRACT.md",
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "~5.8.2"
  },
  "allowScripts": {
    "protobufjs@7.6.5": true
  }
}
